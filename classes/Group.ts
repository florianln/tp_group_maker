class Group {
    firstname(firstname: string): string {
        let value: string;
        value = "John";
        return value;
    }
    lastname(lastname: string): string {
        let value: string;
        value = "Doe";
        return value;
    }
}

export default Group;